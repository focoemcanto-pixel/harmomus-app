"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import type { ReactNode } from "react";
import { CheckCircle2, ChevronLeft, ChevronRight, Circle, CircleHelp, Clock3, FileText, Loader2, Music2, Play, RotateCcw, Users, X } from "lucide-react";

type StudyStatus = "not_studied" | "studied" | "doubt" | "review";
type TeamMember = { id: string; name: string; email?: string | null; isCoordinator?: boolean };
type MinistryPlaylistTrack = { id: string; position: number; name: string; artist: string | null; coverUrl: string | null; href: string; kitId?: string | null; studyStatus?: StudyStatus; preferredVoice?: string | null; preferredTone?: string | null; individualNotes?: string | null; scaleNotes?: string | null; studyMode?: string | null };
type ResolvedAudio = { streamUrl: string | null; tone: string | null; voice: string | null; label: string | null };
type AudioFilesApiFile = { streamUrl?: string | null; url?: string | null; voice?: string | null; name?: string | null; tone?: string | null };
type AudioFilesApiTone = { tone?: string | null; files?: AudioFilesApiFile[] | null };
type AudioCacheEntry = { tones: AudioFilesApiTone[]; resolved: ResolvedAudio };
type LyricsPayload = { name?: string | null; artist?: string | null; lyrics?: string | null };
type MinistryPlaylistPlayerProps = { tracks: MinistryPlaylistTrack[]; repertoireId?: string; updateStudyStatusAction?: (formData: FormData) => Promise<void>; teamMembers?: TeamMember[]; coordinatorName?: string | null };

const STUDY_STATUS_OPTIONS: Array<{ status: StudyStatus; label: string; icon: typeof Circle; className: string; activeClassName: string }> = [
  { status: "not_studied", label: "Não estudada", icon: Circle, className: "border-white/10 bg-white/[0.04] text-zinc-100 hover:bg-white/10", activeClassName: "border-zinc-300/30 bg-zinc-300/10 text-zinc-100" },
  { status: "studied", label: "Estudei OK", icon: CheckCircle2, className: "border-emerald-300/25 bg-emerald-400/10 text-emerald-100 hover:bg-emerald-400/20", activeClassName: "border-emerald-300/40 bg-emerald-400/20 text-emerald-50" },
  { status: "doubt", label: "Tenho dúvida", icon: CircleHelp, className: "border-amber-300/25 bg-amber-400/10 text-amber-100 hover:bg-amber-400/20", activeClassName: "border-amber-300/40 bg-amber-400/20 text-amber-50" },
  { status: "review", label: "Preciso revisar", icon: RotateCcw, className: "border-fuchsia-300/25 bg-fuchsia-400/10 text-fuchsia-100 hover:bg-fuchsia-400/20", activeClassName: "border-fuchsia-300/40 bg-fuchsia-400/20 text-fuchsia-50" },
];
function statusLabel(status: StudyStatus) { return STUDY_STATUS_OPTIONS.find((option) => option.status === status)?.label ?? "Não estudada"; }
function statusBadgeClass(status: StudyStatus) { if (status === "studied") return "border-emerald-300/30 bg-emerald-300/10 text-emerald-100"; if (status === "doubt") return "border-amber-300/30 bg-amber-300/10 text-amber-100"; if (status === "review") return "border-fuchsia-300/30 bg-fuchsia-300/10 text-fuchsia-100"; return "border-white/10 bg-white/[0.04] text-zinc-300"; }
function normalizeVoice(value?: string | null) { return String(value ?? "").normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase().trim(); }
function voiceLabel(value?: string | null) { const normalized = normalizeVoice(value); const map: Record<string, string> = { todos: "Todos", lead: "Lead", tenor: "Tenor", contralto: "Contralto", soprano: "Soprano", baritono: "Barítono", baixo: "Baixo" }; return map[normalized] ?? value ?? "Áudio"; }
function toneLabel(value?: string | null) { return value ? value.replace("#", "♯") : null; }
function protectAudioElement(audio: HTMLAudioElement | null) { if (!audio) return; audio.setAttribute("controlsList", "nodownload noplaybackrate"); audio.setAttribute("disableRemotePlayback", "true"); audio.setAttribute("oncontextmenu", "return false"); }
function allFiles(tones: AudioFilesApiTone[] | null | undefined) { return (tones ?? []).flatMap((tone) => (tone.files ?? []).map((file) => ({ tone: tone.tone ?? file.tone ?? null, file }))); }
function pickAudio(tones: AudioFilesApiTone[] | null | undefined, preferredTone?: string | null, preferredVoice?: string | null): ResolvedAudio { const files = allFiles(tones); const normalizedTone = String(preferredTone ?? "").toUpperCase(); const normalizedVoice = normalizeVoice(preferredVoice); const matchesTone = normalizedTone ? files.filter(({ tone }) => String(tone ?? "").toUpperCase() === normalizedTone) : files; const exactVoice = matchesTone.find(({ file }) => normalizeVoice(file.voice ?? file.name) === normalizedVoice); const voiceContains = matchesTone.find(({ file }) => normalizedVoice && normalizeVoice(file.voice ?? file.name).includes(normalizedVoice)); const todos = matchesTone.find(({ file }) => normalizeVoice(file.voice ?? file.name).includes("todos")); const anyWithTone = matchesTone.find(({ file }) => Boolean(file.streamUrl || file.url)); const any = files.find(({ file }) => Boolean(file.streamUrl || file.url)); const preferred = exactVoice ?? voiceContains ?? todos ?? anyWithTone ?? any; if (!preferred) return { streamUrl: null, tone: null, voice: null, label: null }; return { streamUrl: preferred.file.streamUrl ?? preferred.file.url ?? null, tone: preferred.tone, voice: preferred.file.voice ?? preferred.file.name ?? null, label: preferred.file.name ?? preferred.file.voice ?? null }; }
function uniqueTones(tones?: AudioFilesApiTone[] | null) { return Array.from(new Set((tones ?? []).map((tone) => tone.tone).filter(Boolean))) as string[]; }
function uniqueVoices(tones?: AudioFilesApiTone[] | null) { return Array.from(new Set(allFiles(tones).map(({ file }) => normalizeVoice(file.voice ?? file.name)).filter(Boolean))); }
function ProtectedAudio({ source, cacheKey, autoPlay, onEnded }: { source: string; cacheKey: string; autoPlay?: boolean; onEnded?: () => void }) { const audioRef = useRef<HTMLAudioElement | null>(null); useEffect(() => { protectAudioElement(audioRef.current); if (autoPlay && audioRef.current) audioRef.current.play().catch(() => null); }, [source, autoPlay]); return <audio key={cacheKey} ref={audioRef} controls autoPlay={autoPlay} controlsList="nodownload noplaybackrate" preload="metadata" className="w-full" onEnded={onEnded} onContextMenu={(event) => event.preventDefault()} onLoadedMetadata={(event) => protectAudioElement(event.currentTarget)}><source src={source} /></audio>; }

export function MinistryPlaylistPlayer({ tracks, repertoireId, updateStudyStatusAction, teamMembers = [], coordinatorName }: MinistryPlaylistPlayerProps) {
  const [currentIndex, setCurrentIndex] = useState<number | null>(tracks.length ? 0 : null);
  const [lastPlayedIndex, setLastPlayedIndex] = useState<number | null>(null);
  const [audioCache, setAudioCache] = useState<Record<string, AudioCacheEntry>>({});
  const [audioLoading, setAudioLoading] = useState(false);
  const [audioError, setAudioError] = useState<string | null>(null);
  const [customVoice, setCustomVoice] = useState<Record<string, string>>({});
  const [customTone, setCustomTone] = useState<Record<string, string>>({});
  const [autoPlay, setAutoPlay] = useState(false);
  const [scaleModalOpen, setScaleModalOpen] = useState(false);
  const [pendingModalOpen, setPendingModalOpen] = useState(false);
  const [lyricsModalOpen, setLyricsModalOpen] = useState(false);
  const [lyricsCache, setLyricsCache] = useState<Record<string, LyricsPayload>>({});
  const [lyricsLoading, setLyricsLoading] = useState(false);
  const [lyricsError, setLyricsError] = useState<string | null>(null);

  const currentTrack = currentIndex === null ? null : tracks[currentIndex] ?? null;
  const currentKitId = currentTrack?.kitId ? String(currentTrack.kitId) : null;
  const nextTrack = currentIndex === null ? tracks[0] ?? null : tracks[currentIndex + 1] ?? null;
  const lastPlayedTrack = lastPlayedIndex === null ? null : tracks[lastPlayedIndex] ?? null;
  const studiedCount = tracks.filter((track) => track.studyStatus === "studied").length;
  const pendingTracks = tracks.filter((track) => ["review", "doubt"].includes(track.studyStatus ?? "not_studied"));
  const progressPercent = tracks.length ? Math.round((studiedCount / tracks.length) * 100) : 0;
  const cacheEntry = currentKitId ? audioCache[currentKitId] : null;
  const selectedTone = currentTrack ? customTone[currentTrack.id] || currentTrack.preferredTone || "" : "";
  const selectedVoice = currentTrack ? customVoice[currentTrack.id] || currentTrack.preferredVoice || "todos" : "todos";
  const currentAudio = cacheEntry ? pickAudio(cacheEntry.tones, selectedTone, selectedVoice) : null;
  const playbackLabel = useMemo(() => currentTrack && currentIndex !== null ? `Música ${currentIndex + 1} de ${tracks.length}` : "Pronto para iniciar", [currentIndex, currentTrack, tracks.length]);
  const currentLyrics = currentKitId ? lyricsCache[currentKitId] : null;

  useEffect(() => {
    const kitId = currentTrack?.kitId ? String(currentTrack.kitId) : null;
    if (!kitId || audioCache[kitId]) return;
    let cancelled = false;
    async function loadAudio() {
      if (!kitId) return;
      setAudioLoading(true);
      setAudioError(null);
      try {
        const response = await fetch(`/api/kits/${kitId}/audio-files`, { cache: "no-store" });
        const data = await response.json().catch(() => null);
        if (!response.ok) throw new Error(data?.error || "Não foi possível carregar os áudios desta música.");
        const tones = (data?.tones ?? []) as AudioFilesApiTone[];
        const resolved = pickAudio(tones, currentTrack?.preferredTone, currentTrack?.preferredVoice);
        if (!resolved.streamUrl) throw new Error("Nenhum áudio disponível para esta música.");
        if (!cancelled) setAudioCache((current) => ({ ...current, [kitId]: { tones, resolved } }));
      } catch (error) {
        if (!cancelled) setAudioError(error instanceof Error ? error.message : "Erro ao carregar áudio.");
      } finally {
        if (!cancelled) setAudioLoading(false);
      }
    }
    void loadAudio();
    return () => { cancelled = true; };
  }, [audioCache, currentTrack?.kitId, currentTrack?.preferredTone, currentTrack?.preferredVoice]);

  async function openLyrics() {
    if (!currentKitId) return;
    const kitId = currentKitId;
    setLyricsModalOpen(true);
    setLyricsError(null);
    if (lyricsCache[kitId]) return;
    setLyricsLoading(true);
    try {
      const response = await fetch(`/api/kits/${kitId}/lyrics`, { cache: "no-store" });
      const data = await response.json().catch(() => null);
      if (!response.ok) throw new Error(data?.error || "Não foi possível carregar a letra.");
      setLyricsCache((current) => ({ ...current, [kitId]: data as LyricsPayload }));
    } catch (error) {
      setLyricsError(error instanceof Error ? error.message : "Não foi possível carregar a letra.");
    } finally {
      setLyricsLoading(false);
    }
  }
  function playTrack(index: number) { if (index < 0 || index >= tracks.length) return; setLastPlayedIndex(currentIndex); setCurrentIndex(index); }
  function playPlaylist() { if (!tracks.length) return; setAutoPlay(true); playTrack(currentIndex ?? 0); }
  function goToPrevious() { if (currentIndex === null || currentIndex <= 0) return; playTrack(currentIndex - 1); }
  function goToNext() { if (currentIndex === null || currentIndex >= tracks.length - 1) return; playTrack(currentIndex + 1); }
  function handleAudioEnded() { if (currentIndex !== null && currentIndex < tracks.length - 1) { setAutoPlay(true); goToNext(); } }
  const toneOptions = uniqueTones(cacheEntry?.tones);
  const voiceOptions = uniqueVoices(cacheEntry?.tones);

  return <div className="space-y-6"><div className="rounded-[2rem] border border-cyan-300/20 bg-gradient-to-br from-cyan-400/10 via-white/[0.04] to-fuchsia-500/10 p-5 shadow-[0_24px_90px_rgba(34,211,238,0.12)] md:p-7"><div className="grid gap-6 lg:grid-cols-[260px_minmax(0,1fr)]"><div className="mx-auto flex w-full max-w-[260px] flex-col gap-4"><div className="aspect-square overflow-hidden rounded-[2rem] border border-white/10 bg-black/30 shadow-2xl shadow-cyan-950/30">{currentTrack?.coverUrl ? <img src={currentTrack.coverUrl} alt={currentTrack.name} className="h-full w-full object-cover" /> : <div className="flex h-full w-full items-center justify-center text-cyan-100"><Music2 className="h-16 w-16" /></div>}</div><div className="h-3 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-cyan-300 transition-all" style={{ width: `${progressPercent}%` }} /></div><p className="text-center text-xs font-semibold uppercase tracking-[0.18em] text-zinc-400">{studiedCount}/{tracks.length} OK</p></div><div className="min-w-0"><div className="flex flex-wrap items-center justify-between gap-3"><p className="text-xs font-semibold uppercase tracking-[0.24em] text-cyan-200">{playbackLabel}</p><div className="flex flex-wrap gap-2"><button type="button" onClick={openLyrics} disabled={!currentTrack?.kitId} className="inline-flex items-center gap-2 rounded-xl border border-cyan-300/25 bg-cyan-400/10 px-3 py-2 text-xs font-semibold text-cyan-100 hover:bg-cyan-400/20 disabled:opacity-50"><FileText className="h-4 w-4" /> Letra</button><button type="button" onClick={() => setScaleModalOpen(true)} className="inline-flex items-center gap-2 rounded-xl border border-white/10 px-3 py-2 text-xs font-semibold text-zinc-200 hover:bg-white/10"><Users className="h-4 w-4" /> Visualizar escala</button><button type="button" onClick={() => setPendingModalOpen(true)} className="inline-flex items-center gap-2 rounded-xl border border-fuchsia-300/25 bg-fuchsia-400/10 px-3 py-2 text-xs font-semibold text-fuchsia-100 hover:bg-fuchsia-400/20"><RotateCcw className="h-4 w-4" /> Pendências ({pendingTracks.length})</button></div></div><h3 className="mt-3 text-3xl font-black tracking-tight text-white md:text-5xl">{currentTrack?.name ?? "Playlist ministerial"}</h3><p className="mt-2 text-base text-zinc-300">{currentTrack?.artist ?? "Clique em Reproduzir Escala para iniciar o estudo."}</p><div className="mt-5 flex flex-wrap gap-2 text-xs font-semibold uppercase tracking-[0.16em]"><span className={`rounded-full border px-3 py-1.5 ${statusBadgeClass(currentTrack?.studyStatus ?? "not_studied")}`}>{statusLabel(currentTrack?.studyStatus ?? "not_studied")}</span>{currentTrack?.preferredVoice ? <span className="rounded-full border border-cyan-300/25 bg-cyan-400/10 px-3 py-1.5 text-cyan-100">Nipe designado: {voiceLabel(currentTrack.preferredVoice)}</span> : null}{currentTrack?.preferredTone ? <span className="rounded-full border border-cyan-300/25 bg-cyan-400/10 px-3 py-1.5 text-cyan-100">Tom designado: {toneLabel(currentTrack.preferredTone)}</span> : null}{currentAudio?.voice ? <span className="rounded-full border border-white/10 bg-white/[0.04] px-3 py-1.5 text-zinc-200">Tocando: {voiceLabel(currentAudio.voice)} {toneLabel(currentAudio.tone) ? `• ${toneLabel(currentAudio.tone)}` : ""}</span> : null}</div>{(currentTrack?.individualNotes || currentTrack?.scaleNotes) ? <div className="mt-5 rounded-2xl border border-amber-300/20 bg-amber-400/10 p-4"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-amber-100">Observações</p>{currentTrack.individualNotes ? <p className="mt-2 text-sm leading-6 text-amber-50">Individual: {currentTrack.individualNotes}</p> : null}{currentTrack.scaleNotes ? <p className="mt-1 text-sm leading-6 text-zinc-200">Música: {currentTrack.scaleNotes}</p> : null}</div> : null}<div className="mt-5 grid gap-3 md:grid-cols-2"><label className="block"><span className="text-xs font-semibold uppercase tracking-[0.14em] text-zinc-500">Conferir outra voz</span><select value={selectedVoice} onChange={(event) => currentTrack && setCustomVoice((current) => ({ ...current, [currentTrack.id]: event.target.value }))} className="mt-2 w-full rounded-2xl border border-white/10 bg-black/25 px-4 py-3 text-sm text-white outline-none focus:border-cyan-300/50"><option value="todos">Todos</option>{voiceOptions.filter((voice) => voice !== "todos").map((voice) => <option key={voice} value={voice}>{voiceLabel(voice)}</option>)}</select></label><label className="block"><span className="text-xs font-semibold uppercase tracking-[0.14em] text-zinc-500">Conferir outro tom</span><select value={selectedTone} onChange={(event) => currentTrack && setCustomTone((current) => ({ ...current, [currentTrack.id]: event.target.value }))} className="mt-2 w-full rounded-2xl border border-white/10 bg-black/25 px-4 py-3 text-sm text-white outline-none focus:border-cyan-300/50"><option value="">Tom padrão/disponível</option>{toneOptions.map((tone) => <option key={tone} value={tone}>{toneLabel(tone)}</option>)}</select></label></div><div className="mt-6 rounded-3xl border border-white/10 bg-black/25 p-4">{audioLoading ? <div className="flex items-center gap-2 text-sm text-zinc-300"><Loader2 className="h-4 w-4 animate-spin" /> Carregando áudio definido...</div> : audioError ? <p className="text-sm text-amber-100">{audioError}</p> : currentAudio?.streamUrl ? <ProtectedAudio cacheKey={`${currentTrack?.id}-${currentAudio.streamUrl}-${selectedVoice}-${selectedTone}`} source={currentAudio.streamUrl} autoPlay={autoPlay} onEnded={handleAudioEnded} /> : <p className="text-sm text-zinc-400">Selecione uma música da escala para carregar o áudio aqui.</p>}</div><div className="mt-5 flex flex-wrap items-center gap-2"><button type="button" onClick={goToPrevious} disabled={currentIndex === null || currentIndex <= 0} className="inline-flex items-center gap-2 rounded-2xl border border-white/10 px-4 py-3 text-sm font-semibold text-zinc-200 transition hover:bg-white/10 disabled:cursor-not-allowed disabled:opacity-40"><ChevronLeft className="h-4 w-4" /> Anterior</button><button type="button" onClick={playPlaylist} disabled={!tracks.length} className="inline-flex items-center gap-2 rounded-2xl bg-cyan-300 px-5 py-3 text-sm font-bold text-slate-950 transition hover:bg-cyan-200 disabled:cursor-not-allowed disabled:opacity-50"><Play className="h-4 w-4 fill-current" /> Reproduzir Escala</button><button type="button" onClick={goToNext} disabled={currentIndex === null || currentIndex >= tracks.length - 1} className="inline-flex items-center gap-2 rounded-2xl border border-white/10 px-4 py-3 text-sm font-semibold text-zinc-200 transition hover:bg-white/10 disabled:cursor-not-allowed disabled:opacity-40">Próxima <ChevronRight className="h-4 w-4" /></button></div>{currentTrack && updateStudyStatusAction && repertoireId ? <div className="mt-5 flex flex-wrap gap-2">{STUDY_STATUS_OPTIONS.map((option) => { const Icon = option.icon; const isActive = (currentTrack.studyStatus ?? "not_studied") === option.status; return <form key={option.status} action={updateStudyStatusAction}><input type="hidden" name="repertoire_id" value={repertoireId} /><input type="hidden" name="item_id" value={currentTrack.id} /><input type="hidden" name="kit_id" value={currentTrack.kitId ?? ""} /><input type="hidden" name="study_status" value={option.status} /><button disabled={isActive} className={`inline-flex w-fit items-center justify-center gap-2 rounded-xl border px-3 py-2 text-sm font-semibold transition disabled:cursor-default ${isActive ? option.activeClassName : option.className}`}><Icon className="h-4 w-4" /> {option.label}</button></form>; })}</div> : null}</div></div></div><div className="grid gap-3 rounded-[2rem] border border-white/10 bg-black/20 p-4 md:grid-cols-3"><StatusPill label="Música atual" track={currentTrack} tone="current" /><StatusPill label="Próxima música" track={nextTrack} tone="next" /><StatusPill label="Última estudada" track={lastPlayedTrack} tone="last" /></div><div className="overflow-hidden rounded-3xl border border-white/10 bg-black/20">{tracks.map((track, index) => { const isCurrent = index === currentIndex; const isNext = currentIndex !== null && index === currentIndex + 1; const isLastPlayed = index === lastPlayedIndex; const itemStatus = track.studyStatus ?? "not_studied"; return <button key={track.id} type="button" onClick={() => playTrack(index)} className={`grid w-full gap-4 border-b border-white/10 p-4 text-left transition last:border-b-0 md:grid-cols-[1fr_auto] md:items-center ${isCurrent ? "bg-cyan-300/12 ring-1 ring-inset ring-cyan-300/35" : isNext ? "bg-emerald-400/10" : isLastPlayed ? "bg-fuchsia-400/10" : "bg-transparent hover:bg-white/[0.035]"}`}><div className="flex w-full items-center gap-4"><div className="flex h-14 w-14 shrink-0 items-center justify-center overflow-hidden rounded-2xl border border-white/10 bg-white/5 text-sm font-bold text-zinc-300">{track.coverUrl ? <img src={track.coverUrl} alt={track.name} className="h-full w-full object-cover" /> : track.position}</div><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><span className="text-sm font-semibold text-zinc-400">{track.position}.</span><h3 className="truncate text-lg font-semibold text-white">{track.name}</h3></div><p className="text-sm text-zinc-400">{track.artist || "Música da escala"}</p><div className="mt-2 flex flex-wrap gap-2 text-[11px] font-semibold uppercase tracking-[0.16em]">{isCurrent ? <span className="rounded-full bg-cyan-300 px-2 py-1 text-slate-950">Atual</span> : null}{isNext ? <span className="rounded-full border border-emerald-300/30 bg-emerald-300/10 px-2 py-1 text-emerald-100">Próxima</span> : null}<span className={`rounded-full border px-2 py-1 ${statusBadgeClass(itemStatus)}`}>{statusLabel(itemStatus)}</span>{track.preferredVoice ? <span className="rounded-full border border-cyan-300/20 bg-cyan-400/10 px-2 py-1 text-cyan-100">{voiceLabel(track.preferredVoice)}</span> : null}{track.preferredTone ? <span className="rounded-full border border-cyan-300/20 bg-cyan-400/10 px-2 py-1 text-cyan-100">{toneLabel(track.preferredTone)}</span> : null}</div></div></div><span className="inline-flex w-fit items-center gap-2 rounded-xl border border-white/10 px-4 py-2 text-sm font-semibold text-zinc-200 md:justify-self-end">{isCurrent ? "Tocando agora" : "Estudar aqui"}</span></button>; })}</div>{scaleModalOpen ? <Modal title="Visualizar escala" onClose={() => setScaleModalOpen(false)}><p className="text-sm text-zinc-400">Coordenador vocal: <strong className="text-white">{coordinatorName ?? "Não definido"}</strong></p><div className="mt-4 grid gap-2">{teamMembers.map((member) => <div key={member.id} className="rounded-2xl border border-white/10 bg-white/[0.04] p-4"><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-white">{member.name}</p>{member.isCoordinator ? <span className="rounded-full border border-amber-300/25 bg-amber-400/10 px-2 py-1 text-xs font-semibold text-amber-100">Coordenador vocal</span> : null}</div>{member.email ? <p className="mt-1 text-xs text-zinc-500">{member.email}</p> : null}</div>)}</div></Modal> : null}{pendingModalOpen ? <Modal title="Pendências de estudo" onClose={() => setPendingModalOpen(false)}>{pendingTracks.length ? <div className="grid gap-2">{pendingTracks.map((track) => <button key={track.id} type="button" onClick={() => { setPendingModalOpen(false); playTrack(tracks.findIndex((item) => item.id === track.id)); }} className="rounded-2xl border border-white/10 bg-white/[0.04] p-4 text-left hover:bg-white/10"><p className="font-semibold text-white">{track.position}. {track.name}</p><p className="mt-1 text-xs text-zinc-400">{statusLabel(track.studyStatus ?? "not_studied")}</p></button>)}</div> : <p className="text-sm text-zinc-400">Nenhuma pendência marcada.</p>}</Modal> : null}{lyricsModalOpen ? <Modal title={`Letra • ${currentLyrics?.name ?? currentTrack?.name ?? "Música"}`} onClose={() => setLyricsModalOpen(false)}>{lyricsLoading ? <div className="flex items-center gap-2 text-sm text-zinc-300"><Loader2 className="h-4 w-4 animate-spin" /> Carregando letra...</div> : lyricsError ? <p className="text-sm text-amber-100">{lyricsError}</p> : currentLyrics?.lyrics ? <pre className="max-h-[58dvh] whitespace-pre-wrap rounded-2xl border border-white/10 bg-black/25 p-4 font-sans text-sm leading-7 text-zinc-100">{currentLyrics.lyrics}</pre> : <p className="text-sm text-zinc-400">Este kit ainda não possui letra cadastrada.</p>}</Modal> : null}</div>;
}
function Modal({ title, children, onClose }: { title: string; children: ReactNode; onClose: () => void }) { return <div className="fixed inset-0 z-[2147483647] bg-black/75 p-4 backdrop-blur-md"><button type="button" aria-label="Fechar" onClick={onClose} className="absolute inset-0" /><div className="relative z-10 mx-auto mt-16 max-h-[80dvh] max-w-2xl overflow-y-auto rounded-[2rem] border border-white/10 bg-[#090d18] p-6 text-white shadow-2xl"><div className="flex items-start justify-between gap-4"><h3 className="text-2xl font-black">{title}</h3><button type="button" onClick={onClose} className="rounded-2xl border border-white/10 p-3 text-zinc-300 hover:bg-white/10"><X className="h-5 w-5" /></button></div><div className="mt-5">{children}</div></div></div>; }
function StatusPill({ label, track, tone }: { label: string; track: MinistryPlaylistTrack | null; tone: "current" | "next" | "last" }) { const toneClass = { current: "border-cyan-300/30 bg-cyan-300/10 text-cyan-100", next: "border-emerald-300/30 bg-emerald-300/10 text-emerald-100", last: "border-fuchsia-300/30 bg-fuchsia-300/10 text-fuchsia-100" }[tone]; return <div className={`rounded-2xl border p-3 ${toneClass}`}><p className="flex items-center gap-2 text-[11px] font-semibold uppercase tracking-[0.18em]"><Clock3 className="h-3.5 w-3.5" /> {label}</p><p className="mt-2 truncate text-sm font-semibold text-white">{track?.name ?? "—"}</p><p className="truncate text-xs text-zinc-300">{track?.artist ?? "Aguardando navegação"}</p></div>; }
